.inc "components.txt"

delay_line_test( RS ) :=
{
	VP.VPULSE( @4, @GND, 1, 20n, 200n );
	R2.CR ( @4, @3, 0 );
	L1.CL ( @3, @1, 7n );
	C1.CC ( @1, @2, 1n );
	R1.CR ( @2, @GND, RS  )
};

CIRCUIT := RESOLVE_TRANS( delay_line_test( 1 ) )
