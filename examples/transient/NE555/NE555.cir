
.inc "components.txt"
.inc "ic.txt"

test_NE555() :=
{
  V1.CV( @3, @0, 5 );
  C1.CC( @1, @0, 10u );
  RB.CR( @2, @1, R);
  RA.CR( @3, @2, 1K );
  IC.NE555( @1, @1, @2, @4, @3, @0  ) //@trigger,  @threshold,  @discharge, @output,  @vcc, @gnd
  // P1.PROBE ( @4,@0 )
};

CIRCUIT := RESOLVE_TRANS( test_NE555() )